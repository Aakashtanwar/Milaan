import { Fraunces_600SemiBold, Fraunces_700Bold, Fraunces_900Black } from '@expo-google-fonts/fraunces';
import { DMSans_400Regular, DMSans_500Medium, DMSans_700Bold } from '@expo-google-fonts/dm-sans';

/**
 * Font map consumed by useFonts() in the root layout. Keys MUST match the
 * family names referenced in tokens.ts (fonts.*). Fraunces = editorial serif
 * display; DM Sans = clean UI/body sans.
 */
export const fontMap = {
  Fraunces_600SemiBold,
  Fraunces_700Bold,
  Fraunces_900Black,
  DMSans_400Regular,
  DMSans_500Medium,
  DMSans_700Bold,
};
